{% macro pictureElement(path, alt, recommendedSizes, imageStyle) %}
<picture>
    {% for type in imageFormats.formats %}
        <source
            type="image/{{ type }}"
            srcset="{{ createSrcset("/images/" + path, type) }}"
            sizes="{{ recommendedSizes | createSizes }}"
        >
    {% endfor %}
    <img src="{{ getDefaultSrc("/images/" + path) }}" alt="{{ alt }}" style="{{ imageStyle }}" />
</picture>
{% endmacro %}